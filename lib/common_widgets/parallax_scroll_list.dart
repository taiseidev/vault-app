import 'package:flutter/material.dart';

class ParallaxScrollList extends StatelessWidget {
  const ParallaxScrollList({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
